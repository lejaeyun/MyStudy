//program 1.31: another improper timing construct
for (long total = 0; i = 0; total < DesiredTime; i++)
{
	time(start);
	k = SequentialSearch(a, n[j], 0);
	time(stop);
	total += stop - start;
}
float runTime = (float)(total) / (float)Ii);
